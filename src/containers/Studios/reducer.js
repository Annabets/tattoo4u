import * as constants from './constants';

const initialState = {

};

export function studiosReducer(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
